#include <stdio.h>

void printHelloWorld(int n) {
    for (int i = 1; i <= n; i++ ) {
        printf("[ %d ] Hello world\n" , i);
    }
}

int main() {
    
    int input;
    printf("Enter a number: ");
    scanf("%d", &input);
    printHelloWorld(input);
    
    return 0;
}
